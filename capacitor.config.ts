import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wordlefr.app',
  appName: 'wordle',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
